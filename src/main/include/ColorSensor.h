#pragma once

#include <frc/util/color.h>
/*
#include <rev/ColorSensorV3.h>
#include <rev/ColorMatch.h>
#include "Constants.h"

class ColorSensor{
public:
    ColorSensor();
    ~ColorSensor();

    frc::Color getDetectedColor();

    //returns 'unknown' if no color could be matched
    std::string getApproximateColor();

private:
    rev::ColorSensorV3 m_colorSensor{COLOR_SENSOR_PORT};
    rev::ColorMatch m_colorMatcher;
};
*/